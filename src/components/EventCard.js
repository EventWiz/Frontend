import React from 'react';
import { connect } from 'react-redux';

const EventCard = props => {
  return <div></div>;
};


const mapStateToProps = ({ Reducer }) => ({
  
});

export default connect(null, mapStateToProps)(EventCard);
